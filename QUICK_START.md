# 🚀 Быстрый старт: GPU Mining в ALFIS

## Что было исправлено

1. ✅ **Критический баг**: GPU использовал nonce=0 вместо текущего → **ИСПРАВЛЕНО**
2. ✅ **Оптимизация**: batch_size 8192 → 4096 (оптимально для вашей RTX 4080)
3. ✅ **Async operations**: Уменьшен overhead копирования памяти
4. ✅ **Документация**: Подробные гайды по производительности

## 📥 Обновление кода

```bash
cd ~/Rust/blakeout-gpu2/blakeout-gpu
git pull
```

## 🔨 Компиляция ALFIS с GPU

```bash
cd Alfis-master

# Полная пересборка с GPU поддержкой
cargo clean
cargo build --release --features gpu
```

**Важно:** Убедитесь что `nvcc` доступен:
```bash
nvcc --version
```

Если нет - добавьте в PATH:
```bash
export PATH=/usr/local/cuda/bin:$PATH
export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH
```

## ▶️ Запуск

```bash
./target/release/alfis
```

## ✅ Проверка что GPU работает

При запуске вы должны увидеть в логах:

```
INFO GPU miner initialized successfully with batch size 4096
INFO Thread 0 using GPU for mining
```

Каждые 10 секунд:
```
INFO GPU mining speed: 1682 H/s, max difficulty: 15, target: 20
```

## 📊 Ожидаемая производительность

**Ваша RTX 4080 SUPER:**
- **~1,682 H/s** (вместо 450 H/s на CPU)
- **3.7x ускорение** против 12-поточного CPU
- **8GB VRAM** используется

## 🔧 Настройка (опционально)

### Изменить batch_size

Если хотите экспериментировать, отредактируйте `Alfis-master/src/gpu_miner.rs`:

```rust
batch_size: 4096,  // 2048 для 4GB VRAM, 8192 для 16GB VRAM
```

Затем пересоберите:
```bash
cargo build --release --features gpu
```

### Отключить GPU

Скомпилируйте без feature:
```bash
cargo build --release
```

## 🐛 Если что-то не работает

### GPU не обнаружен

```bash
# Проверьте драйвер
nvidia-smi

# Проверьте CUDA
nvcc --version

# Если не работает - установите CUDA Toolkit
# https://developer.nvidia.com/cuda-downloads
```

### Ошибки компиляции

```bash
# Убедитесь что путь к CUDA правильный
export CUDA_PATH=/usr/local/cuda

# Пересоберите blakeout-gpu
cd blakeout-gpu
cargo clean
cargo build --release

# Затем ALFIS
cd ../Alfis-master
cargo clean
cargo build --release --features gpu
```

### GPU slower than expected

- Проверьте температуру: `nvidia-smi` (должно быть <80°C)
- Закройте другие GPU приложения
- Убедитесь что batch_size=4096

## 📈 Мониторинг

### Во время майнинга откройте второй терминал:

```bash
watch -n 1 nvidia-smi
```

Смотрите на:
- **GPU-Util**: должно быть ~95-100%
- **Memory-Usage**: ~8000MB / ~16000MB
- **Temp**: <80°C
- **Power**: близко к максимуму (200-300W)

## 📚 Подробная документация

- **ALFIS_GPU_INTEGRATION.md** - полное руководство по интеграции
- **PERFORMANCE.md** (в blakeout-gpu/) - анализ производительности
- **TEST_INSTRUCTIONS.md** (в blakeout-gpu/) - тестирование standalone

## 🎯 Следующие шаги

1. Запустите ALFIS с GPU
2. Проверьте логи на "GPU miner initialized"
3. Подождите нахождения блока
4. Сравните с CPU производительностью

**Ожидаемый результат:** GPU найдет блок в **~3.7x быстрее** чем CPU!

---

## 💡 Почему не больше ускорения?

Blakeout **специально спроектирован** быть GPU-resistant:
- 65,536 последовательных итераций Blake2s
- Каждая итерация ждет предыдущую
- Невозможно распараллелить внутри одного хеша

GPU может параллелить **разные nonce**, но не операции **внутри хеша**.

**3.7x - это отличный результат для memory-hard алгоритма!** 🎉

Подробнее: `blakeout-gpu/PERFORMANCE.md`
